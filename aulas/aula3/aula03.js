"use strict";
//Variáveis

var nome = "João";
let idade = 25;
const altura = 1.75;
console.log("Esse é o " + nome + ", ele tem " + idade + " anos e " + altura + " metros de altura.");

/*
Ao usar o var, a variavel pode ser redeclarada e atualizada dentro do mesmo escopo. Já o let não permite redeclaração no mesmo escopo, mas permite atualização. Ou seja, dentro de um bloco a variavel var pode ser declarada fora dele, enquanto o let não pode.
*/